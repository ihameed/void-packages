--- a/NVIDIA-Linux-x86_64-515.86.01/kernel/common/inc/nv-linux.h	2024-02-05 20:30:32.008452827 -0800
+++ b/NVIDIA-Linux-x86_64-515.86.01/kernel/common/inc/nv-linux.h	2024-02-05 20:32:21.558717297 -0800
@@ -2068,4 +2068,26 @@
 }
 #endif
 
+#if defined(CONFIG_HAVE_ARCH_PFN_VALID) || LINUX_VERSION_CODE < KERNEL_VERSION(6,1,76)
+#  define nv_pfn_valid pfn_valid
+#else
+/* pre-6.1.76 kernel pfn_valid version without GPL rcu_read_lock/unlock() */
+static inline int nv_pfn_valid(unsigned long pfn)
+{
+        struct mem_section *ms;
+
+        if (PHYS_PFN(PFN_PHYS(pfn)) != pfn)
+                return 0;
+
+        if (pfn_to_section_nr(pfn) >= NR_MEM_SECTIONS)
+                return 0;
+
+        ms = __pfn_to_section(pfn);
+        if (!valid_section(ms))
+                return 0;
+
+        return early_section(ms) || pfn_section_valid(ms, pfn);
+}
+#endif
+
 #endif  /* _NV_LINUX_H_ */
--- a/NVIDIA-Linux-x86_64-515.86.01/kernel/nvidia/nv-mmap.c	2024-02-05 20:30:32.008452827 -0800
+++ b/NVIDIA-Linux-x86_64-515.86.01/kernel/nvidia/nv-mmap.c	2024-02-05 20:32:21.558717297 -0800
@@ -587,7 +587,7 @@
             //
             if ((nv_get_numa_status(nvl) == NV_NUMA_STATUS_ONLINE) &&
                 !IS_REG_OFFSET(nv, access_start, access_len) &&
-                (pfn_valid(PFN_DOWN(mmap_start))))
+                (nv_pfn_valid(PFN_DOWN(mmap_start))))
             {
                 ret = nvidia_mmap_numa(vma, mmap_context);
                 if (ret)
--- a/NVIDIA-Linux-x86_64-515.86.01/kernel/nvidia/os-mlock.c	2024-02-05 20:30:32.008452827 -0800
+++ b/NVIDIA-Linux-x86_64-515.86.01/kernel/nvidia/os-mlock.c	2024-02-05 20:32:21.558717297 -0800
@@ -113,7 +113,7 @@
     for (i = 0; i < page_count; i++)
     {
         if ((nv_follow_pfn(vma, (start + (i * PAGE_SIZE)), &pfn) < 0) ||
-            (!pfn_valid(pfn)))
+            (!nv_pfn_valid(pfn)))
         {
             rmStatus = NV_ERR_INVALID_ADDRESS;
             break;
@@ -187,7 +187,7 @@
         goto done;
     }
 
-    if (pfn_valid(pfn))
+    if (nv_pfn_valid(pfn))
     {
         rmStatus = get_io_pages(vma, start, page_count, (struct page **)result_array);
         if (rmStatus == NV_OK)

